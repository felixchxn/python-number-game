def guess_number():
    number = 68
    attempts = 0

    while True:
        guess = int(input("Guess the number 68: "))
        attempts += 1

        if guess == number:
            print("Congratulations! You guessed the correct number in", attempts, "attempts.")
            break
        else:
            print("Wrong guess. Try again.")

guess_number()
